using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface EquipmentInterface
{
    public enum eType
    {
        None = 0,
        Sword = 1,
        Shield = 2,
    }
}
